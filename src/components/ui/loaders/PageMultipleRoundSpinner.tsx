import { Center } from "@chakra-ui/react";

import MultipleRoundSpinner from "./MultipleRoundSpinner";

export default function PageMultipleRoundSpinner() {
    return (
        <Center w="full" h="full" bgColor="chakra-body-bg" borderRadius="lg">
            <MultipleRoundSpinner />
        </Center>
    );
}
